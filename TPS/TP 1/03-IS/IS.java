import java.util.Scanner;

public class IS {
    public static boolean Vogais(String s) {
    boolean resultado = "";
    // Percorre 
    for (int i = 0; i < s.length(); i++) {
        char c = s.charAt(i);

        if (c == 'a' || c == 'e' || 'i' || 'o' || 'u') {
            
        }
    }
    }
}